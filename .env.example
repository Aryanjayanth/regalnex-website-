# Server Configuration
PORT=3001                           # Port number for the server to run on
CORS_ORIGIN=http://localhost:5173   # Frontend development server URL

# Email Configuration
SMTP_HOST=smtp.gmail.com            # SMTP server host
SMTP_PORT=587                       # SMTP server port
SMTP_SECURE=false                   # Use TLS for SMTP
EMAIL_USER=your-email@gmail.com     # Your Gmail address
EMAIL_PASS=your-app-password        # Gmail app-specific password
OWNER_EMAIL=business@regalnex.com   # Business email to receive notifications

# API Keys and External Services
VITE_SOME_API_KEY=your-api-key      # Add any external API keys needed

# Development/Production Mode
NODE_ENV=development                # Environment mode (development/production)

# Note: Never commit the actual .env file with real values to version control
# Copy this file to .env and fill in your actual values